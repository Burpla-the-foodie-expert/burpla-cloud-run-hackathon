# This file specifies files that are *not* uploaded to Google Cloud when
# running gcloud commands. It follows the same syntax as .gitignore.

**/node_modules
**/build

# Default gcloudignore file generated by gcloud init.
.gcloudignore
# Dockerfile is explicitly included in the build context.
!Dockerfile

# Other files to ignore
npm-debug.log
.DS_Store
